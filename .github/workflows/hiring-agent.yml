name: Hiring Agent

on:
  schedule:
    - cron: '*/10 * * * *'  # every 10 minutes
  workflow_dispatch:         # allow manual trigger from GitHub UI

jobs:
  run-agent:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Syntax check
        run: python -m compileall -q .

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Resolve installed Playwright version
        id: pw
        run: echo "version=$(python -c "import importlib.metadata as m; print(m.version('playwright'))")" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          # Key on the actual installed Playwright version, not requirements.txt.
          # playwright is unpinned (>=), so pip upgrades it silently; keying on the
          # resolved version means a browser-build bump busts the cache and forces a
          # fresh `playwright install`, instead of restoring a stale browser the new
          # Playwright can't launch.
          key: playwright-chromium-${{ steps.pw.outputs.version }}

      - name: Install Playwright + system deps
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: playwright install --with-deps chromium

      - name: Install Playwright system deps (cache hit)
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run: playwright install-deps chromium

      - name: Verify Playwright browser is present
        run: python -c "from playwright.sync_api import sync_playwright; p=sync_playwright().start(); b=p.chromium.launch(); b.close(); p.stop(); print('chromium OK')"

      - name: Run hiring agent
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CAREERPLUG_EMAIL: ${{ secrets.CAREERPLUG_EMAIL }}
          CAREERPLUG_PASSWORD: ${{ secrets.CAREERPLUG_PASSWORD }}
          CAREERPLUG_COMPANY_SLUG: ${{ secrets.CAREERPLUG_COMPANY_SLUG }}
          TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
          TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}
          TWILIO_FROM_NUMBER: ${{ secrets.TWILIO_FROM_NUMBER }}
          TWILIO_MESSAGING_SERVICE_SID: ${{ secrets.TWILIO_MESSAGING_SERVICE_SID }}
          TWILIO_TO_NUMBER: ${{ secrets.TWILIO_TO_NUMBER }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          GRAPH_TENANT_ID: ${{ secrets.GRAPH_TENANT_ID }}
          GRAPH_CLIENT_ID: ${{ secrets.GRAPH_CLIENT_ID }}
          GRAPH_CLIENT_SECRET: ${{ secrets.GRAPH_CLIENT_SECRET }}
          GRAPH_USER_EMAIL: ${{ secrets.GRAPH_USER_EMAIL }}
          EMAIL_TRIGGER_SUBJECT: ${{ secrets.EMAIL_TRIGGER_SUBJECT }}
          CALENDLY_LINK: ${{ secrets.CALENDLY_LINK }}
          CALENDLY_LINK_1HR: ${{ secrets.CALENDLY_LINK_1HR }}
          SCORE_NOTIFY_THRESHOLD: ${{ secrets.SCORE_NOTIFY_THRESHOLD }}
        run: python main.py

      - name: Run follow-up agent
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CAREERPLUG_EMAIL: ${{ secrets.CAREERPLUG_EMAIL }}
          CAREERPLUG_PASSWORD: ${{ secrets.CAREERPLUG_PASSWORD }}
          CAREERPLUG_COMPANY_SLUG: ${{ secrets.CAREERPLUG_COMPANY_SLUG }}
          TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
          TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}
          TWILIO_FROM_NUMBER: ${{ secrets.TWILIO_FROM_NUMBER }}
          TWILIO_MESSAGING_SERVICE_SID: ${{ secrets.TWILIO_MESSAGING_SERVICE_SID }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          GRAPH_TENANT_ID: ${{ secrets.GRAPH_TENANT_ID }}
          GRAPH_CLIENT_ID: ${{ secrets.GRAPH_CLIENT_ID }}
          GRAPH_CLIENT_SECRET: ${{ secrets.GRAPH_CLIENT_SECRET }}
          GRAPH_USER_EMAIL: ${{ secrets.GRAPH_USER_EMAIL }}
          CALENDLY_LINK: ${{ secrets.CALENDLY_LINK }}
          MANAGER_PHONE: ${{ secrets.MANAGER_PHONE }}
        run: python follow_up.py

      - name: Run scheduling agent
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CAREERPLUG_EMAIL: ${{ secrets.CAREERPLUG_EMAIL }}
          CAREERPLUG_PASSWORD: ${{ secrets.CAREERPLUG_PASSWORD }}
          CAREERPLUG_COMPANY_SLUG: ${{ secrets.CAREERPLUG_COMPANY_SLUG }}
          TWILIO_ACCOUNT_SID: ${{ secrets.TWILIO_ACCOUNT_SID }}
          TWILIO_AUTH_TOKEN: ${{ secrets.TWILIO_AUTH_TOKEN }}
          TWILIO_FROM_NUMBER: ${{ secrets.TWILIO_FROM_NUMBER }}
          TWILIO_MESSAGING_SERVICE_SID: ${{ secrets.TWILIO_MESSAGING_SERVICE_SID }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          GRAPH_TENANT_ID: ${{ secrets.GRAPH_TENANT_ID }}
          GRAPH_CLIENT_ID: ${{ secrets.GRAPH_CLIENT_ID }}
          GRAPH_CLIENT_SECRET: ${{ secrets.GRAPH_CLIENT_SECRET }}
          GRAPH_USER_EMAIL: ${{ secrets.GRAPH_USER_EMAIL }}
          CALENDLY_LINK: ${{ secrets.CALENDLY_LINK }}
          CLUBREADY_USERNAME: ${{ secrets.CLUBREADY_USERNAME }}
          CLUBREADY_PASSWORD: ${{ secrets.CLUBREADY_PASSWORD }}
        run: python schedule_interviews.py
